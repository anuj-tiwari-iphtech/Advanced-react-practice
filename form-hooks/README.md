# Student Registration Form

A simple **Student Registration Form** built using **React** and **React Hook Form**.

## Features

- Student Registration Form
- React Hook Form
- Live Input Preview using `watch()`
- Programmatically Update Fields using `setValue()`
- Retrieve Form Data using `getValues()`
- Reset Form using `reset()`
- Controlled Component using `Controller`
- Validation Error Messages

## Hooks Used

- `useForm()`
- `register()`
- `handleSubmit()`
- `watch()`
- `setValue()`
- `getValues()`
- `reset()`
- `formState.errors`
- `Controller`

---

## Prerequisites

Make sure you have installed:

- Node.js
- npm 

Check versions:

```bash
node -v
npm -v
```

---

## Installation

### 1. Clone the repository

```bash
git clone https://github.com/anuj-tiwari-iphtech/Advanced-react-practice.git
```

### 2. Navigate to the project folder

```bash
cd form-hooks
```

### 3. Install dependencies

```bash
npm install
```


### 4. Start the development server

```bash
npm run dev
```

If you're using Create React App:

```bash
npm start
```

## Technologies Used

- React
- React Hook Form
- JavaScript
- HTML
- CSS

---